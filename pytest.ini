[pytest]
addopts =
        --cov
        --cov-report term
        --cov-report html
        --no-cov-on-fail
        --strict-markers
        -Werror
        -W ignore::FutureWarning

testpaths = tests

junit_family = xunit2
